import { daysToMs, hoursToMs, minutesToMs, secondsToMs } from './functions';

export class DateUtility {
  static secondsToMs = secondsToMs;

  static minutesToMs = minutesToMs;

  static hoursToMs = hoursToMs;

  static daysToMs = daysToMs;
}
